<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">
        <activity android:name=".ui.routers.RouterFormActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".ui.switches.SwitchFormActivity" />
        <activity android:name=".ui.switches.SwitchListActivity" />
        <activity android:name=".ui.aps.APFormActivity" />
        <activity android:name=".ui.aps.APListActivity" />
        <activity android:name=".ui.report.ReportActivity" />

        <activity android:name=".ui.routers.RouterListActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
